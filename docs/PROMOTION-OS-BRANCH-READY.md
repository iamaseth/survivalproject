# Promotion OS branch ready

The `promotion-os-foundation` branch is ready for review as a draft PR. It contains shell/navigation changes and documentation only; no production database migrations are included.
